CREATE TABLE [model].[Device Event] (
    [DeviceEventKey] INT          NOT NULL,
    [Event Source]   VARCHAR (50) NOT NULL,
    [Event Sub Type] VARCHAR (50) NOT NULL,
    [Event Type]     VARCHAR (50) NOT NULL
);

