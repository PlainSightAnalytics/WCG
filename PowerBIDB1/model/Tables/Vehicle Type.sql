CREATE TABLE [model].[Vehicle Type] (
    [VehicleTypeKey]        INT          NOT NULL,
    [Vehicle Category]      VARCHAR (50) NOT NULL,
    [Vehicle Category Code] VARCHAR (10) NOT NULL,
    [Vehicle Usage]         VARCHAR (50) NOT NULL,
    [Vehicle Usage Code]    VARCHAR (10) NOT NULL
);

