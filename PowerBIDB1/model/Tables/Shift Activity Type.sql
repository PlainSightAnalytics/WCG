CREATE TABLE [model].[Shift Activity Type] (
    [ShiftActivityTypeKey]     INT          NOT NULL,
    [Shift Activity Type]      VARCHAR (30) NOT NULL,
    [Shift Activity Type Code] INT          NOT NULL
);

